# Update v1.3 -- orbit results banked; figure layout fix

Doc: measured-status gains the flagship paragraph + orbit figure
(arrival render vs actual photo, chord trajectory).  Recorded findings:
antipodal self-consistency bump (reference anchoring visible in a real
capture), 31/31 exact reloc (orbit = maximally distinct views, the
inverse of LLFF), floor invariance across all backbones/captures, and
the pi^3 coverage caveat (mask policy).

Code: run_orbit_demo.py figure layout only (colorbar no longer collides
with the trajectory panel).  Re-run either orbit command if you want the
clean figure; numbers are unaffected.

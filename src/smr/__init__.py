"""SMR: scene mental rotation with emergent attractor dynamics.

Sub-packages: dynamics (Amari fields, scaffold state), memory (Vector-HaSH
loop, RLS binding, remapping, novelty), backbones (frozen geometry models),
scene (synthetic rooms, surfel splatting), pipeline (bind / rotate), viz.
"""
__version__ = "0.1.0"
